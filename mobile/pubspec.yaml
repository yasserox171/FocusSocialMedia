name: focus_social
description: تطبيق فوكس سوشيال — المنصة الداخلية لأعضاء جمعية فوكس
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  http: ^1.2.0
  provider: ^6.1.0
  shared_preferences: ^2.2.0
  web_socket_channel: ^2.4.0
  url_launcher: ^6.3.0
  intl: any

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
